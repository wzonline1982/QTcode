#ifndef MAT_H
#define MAT_H

#include <stdio.h>
#include <math.h>
#include <random>
#include <time.h>

#define full 0
#define same 1
#define valid 2

typedef struct Mat2DSize{
    int c; // 列（宽）
    int r; // 行（高）
}nSize;

class mat
{
public:
    mat();


};

#endif // MAT_H
